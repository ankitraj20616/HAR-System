# Sensor simulator

This directory is the stable home for software sensor replay. Milestone 1 only establishes the
repository boundary and shared `SensorRaw` contract; dataset loading and MQTT replay are implemented
in the sensor-pipeline milestone.

Downloaded datasets must be stored under `data/` and are intentionally excluded from version control.
